<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.library.mapper.UserMapper">

    <!-- 通用查询结果映射 -->
    <resultMap id="BaseResultMap" type="com.library.entity.User">
        <id     property="id"         column="id"/>
        <result property="username"   column="username"/>
        <result property="password"   column="password"/>
        <result property="realName"   column="real_name"/>
        <result property="phone"      column="phone"/>
        <result property="email"      column="email"/>
        <result property="role"       column="role"/>
        <result property="status"     column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM `user` WHERE `username` = #{username}
    </select>

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM `user` WHERE `id` = #{id}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.library.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (`username`, `password`, `real_name`, `phone`, `email`, `role`, `status`)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{role}, #{status})
    </insert>

    <!-- 更新用户信息 -->
    <update id="updateById" parameterType="com.library.entity.User">
        UPDATE `user`
        <set>
            <if test="realName != null">`real_name` = #{realName},</if>
            <if test="phone != null">`phone` = #{phone},</if>
            <if test="email != null">`email` = #{email},</if>
            <if test="password != null">`password` = #{password},</if>
            <if test="status != null">`status` = #{status},</if>
        </set>
        WHERE `id` = #{id}
    </update>

    <!-- 分页查询读者列表（role=0） -->
    <select id="selectReaderPage" resultMap="BaseResultMap">
        SELECT * FROM `user`
        WHERE `role` = 0
        ORDER BY `create_time` DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计读者总数 -->
    <select id="countReaders" resultType="java.lang.Long">
        SELECT COUNT(*) FROM `user` WHERE `role` = 0
    </select>

</mapper>
