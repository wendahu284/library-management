<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.library.entity.SystemConfig">
        <id property="id" column="id"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="description" column="description"/>
        <result property="editable" column="editable"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM system_config ORDER BY id
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <update id="updateByKey">
        UPDATE system_config SET config_value = #{configValue} WHERE config_key = #{configKey}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_config (config_key, config_value, description, editable)
        VALUES (#{configKey}, #{configValue}, #{description}, #{editable})
    </insert>

</mapper>
